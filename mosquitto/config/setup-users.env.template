# Copy to setup-users.env (see .gitignore) and set real passwords.
# One MQTT user per line: username=password
# Lines starting with # and blank lines are ignored.
# Passwords may contain = characters; only the first = on the line separates name from password.

# reader=change-me
# publisher=change-me
